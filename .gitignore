__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
.coverage
htmlcov/
# Local backup dirs created at runtime next to a config (never the bundled data).
.backup/

# omo checkout used by --refresh-omo (CI clones it here; belt-and-braces next to the
# refresh workflow's add-paths — never redistribute omo source, only the derived snapshot).
oh-my-openagent/

# Local Claude Code agent state (per-project/subagent memory, etc.) — never publish.
.claude/
